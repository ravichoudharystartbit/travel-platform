{
  "name": "@travel-platform/shared-utils",
  "version": "1.0.0",
  "private": true,
  "description": "Shared utilities, types, and hooks for travel platform",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "npx --package=typescript@5.3.3 tsc --watch",
    "build": "npx --package=typescript@5.3.3 tsc --project tsconfig.json",
    "clean": "rm -rf dist",
    "lint": "eslint src --ext .ts,.tsx"
  },
  "dependencies": {
    "react": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^18"
  },
  "peerDependencies": {
    "next": ">=14.0.0",
    "react": ">=18.0.0"
  }
}